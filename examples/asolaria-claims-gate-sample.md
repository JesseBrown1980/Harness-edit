# Asolaria Claims Gate Sample

This sample exists only to exercise the harness. It is not the system law.

- For security findings, ground impact before severity: inspect usage and distinguish a trust root from a non-anchor, self-regenerating, or example key.
- Preserve 60D HyperBEHCS routing. Do not flatten selector logic; preserve axes, glyph/language systems, proof tiers, route/room, executor, pipe type, and runtime mode.
- For maps, Windows report HTML is a mirror, not authority. Ask fabric/canon first, then use Linux, Ubuntu, WSL, USB, or raw substrate tools before publishing a mirror.
- For cylinder counts, count distinct cylinder or tower identities separately from the Z level axis and max-index fields.
- For PR claims, use GitHub or the owning CI gate. A pasted transcript is a lead, not proof.
- For ledger parity, missing or unreadable files are a hard boundary: report ok=0 and missing=1 or an error, distinct from readable-empty ledgers.
- For substrate claims, fabric first and the owning substrate decide. Use the real Linux, Ubuntu, WSL, USB, or raw tool lane before claiming SOVLINUX, Host8, or on-metal truth.
- For live claims, separate source, built artifact, running process, and live route response. Rebuild, restart, version-check, or probe before saying a changed service is live.
- For sub-agent waves, inject the AGENT-BRIEF/read-first brief into every fresh helper before spawn; unbriefed sub-agents get walled by Windows/PowerShell/SetTokenInformation, so require the Linux/Ubuntu/WSL/USB-raw lane.
